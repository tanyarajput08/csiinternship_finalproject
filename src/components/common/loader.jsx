const Loader = () => {
  return (
    <div style={{
      minHeight: '100vh',
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
      color: 'var(--primary)',
      fontSize: '1.5rem'
    }}>
      Loading Delivera...
    </div>
  );
};

export default Loader;
